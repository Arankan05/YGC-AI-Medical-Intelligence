DATABASE_URL=
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_JWT_SECRET=
SUPABASE_STORAGE_BUCKET=medical-documents
AI_PROVIDER=gemini
AI_MODEL=gemini-3.5-flash-lite
AI_API_KEY=
# Path to Tesseract OCR executable (e.g. C:\Program Files\Tesseract-OCR\tesseract.exe on Windows, /usr/bin/tesseract on Linux)
TESSERACT_CMD=
NOMINATIM_URL=https://nominatim.openstreetmap.org
OVERPASS_URL=https://overpass-api.de/api/interpreter
# Real contact URL or mailto: sent in the User-Agent to Nominatim and Overpass,
# as their usage policies request. Set this to a genuine address before using
# the public instances - it is how they reach us instead of blocking us.
PROVIDER_DIRECTORY_CONTACT=
